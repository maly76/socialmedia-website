<div class="card">
    <div class="card-header">
        <% if current_user == comment.user || current_user == comment.post.user %>
            <ul class="nav card-header-tabs">
                <li class="nav-item">
                    <a class="nav-link" data-method="delete" 
                       data-confirm="Are you sure"
                       href="<%= post_comment_url(@post, comment) %>">Löschen
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link"
                       href="<%= edit_post_comment_path(@post, comment) %>">
                       Bearbeiten
                    </a>
                </li>
            </ul>
        <% end %>
    </div>
    <div class="card-body">
        <h5 class="card-title"><%= comment.user.username %></h5>
        <p class="card-text"><%= comment.body %></p>
    </div>
</div>